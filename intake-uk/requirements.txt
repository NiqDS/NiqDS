# Core — everything needed to run the app and the rules engine offline on the
# default `mock` backend. No API keys, no network at runtime.
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
pydantic>=2.6
pyyaml>=6.0
python-multipart>=0.0.9
itsdangerous>=2.1  # signed session cookies for the scan app login

# Document ingestion / fixture generation.
pdfplumber>=0.10
pypdfium2>=4.25
Pillow>=10.0
reportlab>=4.0

# Tests.
pytest>=8.0

# Optional real LLM backends live in requirements-optional.txt — install them
# only when switching LLM_BACKEND to `anthropic` or `openai`.
